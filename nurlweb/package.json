{
  "name": "nurl-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "facts": "bash ../tools/gen-site-facts.sh",
    "sync-installers": "cp ../tools/get-nurl.sh public/install.sh && cp ../tools/get-nurl.ps1 public/install.ps1",
    "bench-table": "node ../tools/gen-bench-table.mjs",
    "predeploy": "bash ../tools/gen-site-facts.sh && npm run bench-table && npm run sync-installers",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.15.0"
  }
}
