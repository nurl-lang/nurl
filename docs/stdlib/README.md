# Standard library modules

Module-specific standard library documentation lives in this directory.
Each guide uses the module's lowercase name, matching its `.nu` source file.

- [Protocol Buffers](protobuf.md) — checked binary wire readers and writers,
  ownership, limits, examples and interoperability tests.
