[package]
name = "nurllama"
version = "0.17.3"
description = "Run language models locally, in pure NURL — an ollama-shaped engine built package by package on the gguf container toolkit and the gpu compute package. `nurllama run org/repo/model.gguf` fetches the model on demand: run/chat/convert take a local file, a store name, or a Hugging Face ref pulled into the shared ~/.nurl/models cache via the hub package, so there is no separate download step. Architectures: llama, qwen2, gemma3 and phi3, with every detail that changes the output read from the model's own metadata rather than guessed — get one wrong and a model still runs while quietly producing nonsense. gemma3 is a different shape throughout: the embedding row is scaled by sqrt(n_embd), head_dim is stated outright rather than being n_embd/n_head (gemma3-270m: 640/4 = 160, but head_dim is 256), Q and K are RMSNormed per head before the rotation, every block norms its own output before the residual add, the FFN gate is GeGLU, and five of every six layers attend only within a 512-token sliding window using their own smaller RoPE base. Verified against HF transformers running the original safetensors: every layer's hidden state matches to cosine 1.0000, logits correlate at 0.9996 with the same top-1, and the greedy continuation is text-identical. phi3 is the llama shape with its weights FUSED — Q, K and V arrive as one tensor and the FFN gate and up as one — and nothing is split apart to run them: a weight is a device pointer plus a row count, so the parts are row ranges inside the uploaded tensor, and every range lands on a block boundary because a row is a whole number of quantisation blocks (checked against HF transformers reading the same file: logits correlate at 1.000000, identical top-5, same greedy ids) — on the CUDA backend, on the CPU/OpenMP backend, and against a numpy reference reading the same GGUF. Chat templates now tokenise correctly too: a template writes its turn markers (<start_of_turn>, <|im_start|>) into the prompt as text, and nurllama emits each as its own token id the way llama.cpp does — tokenised as ordinary text they shred into a dozen pieces and the model answers a question you did not ask. Decode is fast: warp-per-row matvecs that decode GGUF blocks inside the matmul (Q4_0/Q4_1/Q5_0/Q5_1/Q8_0/Q4_K/Q5_K/Q6_K), so weights stay quantised on the device (~3x less memory than the f32 expansion) and a Q4_K_M model runs at 356 tok/s (SmolLM-135M) or 249 tok/s (Qwen2.5-0.5B) on an RTX 4090. Prompts are processed in batched chunks — matmuls and attention over 64 positions at once — so a 301-token prompt reaches its first generated token in 1.2 s instead of 7.2 s. The tokenizer loads entirely from the model's own tokenizer.ggml.* metadata (SentencePiece bigram merging for llama-family, byte-level BPE for gpt2-family), the store pulls models from Hugging Face with resumable, constant-memory downloads and sha256 verification, and the HTTP API is ollama-compatible (POST /api/generate, /api/chat, GET /api/tags, POST /api/show) so existing clients work unchanged. New in 0.7.0: weights can come from a safetensors file — the container Hugging Face ships every model in — instead of the GGUF's own tensors (run model.gguf PROMPT --weights model.safetensors; the GGUF still supplies the hyperparameters and the tokenizer, which safetensors does not carry). Running a model straight from its checkpoint reproduces HF transformers' own logits to max|delta| = 1.1e-4 with r = 1.00000000 and an identical top-5, where the quantised GGUF path on the same model differs by 1.19 — so an un-quantised HF model now runs unchanged, and the safetensor reader is proven by a whole forward pass rather than by assertion. One subtlety it surfaced: gemma's `1 + w` norm weights are a property of the CONTAINER, not the architecture — the GGUF converter folds the +1 into the stored tensor and the checkpoint does not, so the fold happens on the safetensors path only. New in 0.8.0: the tokenizer engine moved out into its own package (tokenizer) — the same SentencePiece and byte-level BPE that fed llama, qwen2, gemma3 and phi3, now loadable from a GGUF's metadata OR from Hugging Face's tokenizer.json, so whisper and any HF checkpoint can use it too. nurllama's own loader is the 128 lines that read tokenizer.ggml.*; everything else is the package, and its parity tests against independent python SentencePiece and BPE implementations still pass token for token. New in 0.9.0: DIFFUSION language models — the llada2 architecture (LLaDA2.x MoE: bidirectional attention, an fp32 sigmoid router with selection-only bias and group-limited top-k picking 8 of 256 fused quantised experts plus an always-on shared expert, per-head Q/K norms, partial NEOX rotary) and its block-denoise decode: generation fills a template of mask tokens block by block, commits every position that clears a confidence threshold in parallel, and REVISES already-committed tokens the model changes its mind about (LLaDA2.1 editing); completed blocks freeze their K/V so a step costs O(window) where the reference recomputes the whole prefix — proven to produce ids IDENTICAL to a reference-faithful python loop. `nurllama convert` turns a Hugging Face checkpoint directory (safetensors shards + tokenizer.json) into a llama.cpp-convention GGUF, streaming at constant memory (a 32 GB checkpoint converts on a 16 GB machine), verified tensor-for-tensor against the source with expert-fusion order, BF16 widening and Q8_0 error bounds checked, and an f32 conversion matches an independent numpy forward to max-delta 1e-6. The bailingmoe2 (Ling 2.0 / LLaDA2) tokenizer pre-split is token-for-token equal to HF tokenizers on a multilingual battery. New in 0.10.0: `nurllama start` — an interactive setup wizard that lists the models in a directory (and the pull store), asks whether the server is localhost-only or reachable from the network, whether a network server is open or gated by a bearer token, and the port, then writes the choices to $NURLLAMA_HOME/config.json and launches; a second `start` offers to reuse the config and `start -y` reuses it unprompted. The server it launches hosts a self-contained web chat UI (served at / to a browser, while / still returns the ollama status line to a programmatic client): each browser is issued a GUID in an nl_client cookie, and its conversations — every turn, both sides — are persisted to a SQLite database ($NURLLAMA_HOME/history.db) scoped to that GUID, so /web/conversations only ever returns the caller's own chats. A token-auth server serves the page but gates every /web/* and /api/* endpoint behind Authorization: Bearer, which the UI prompts for once. CLI: pull, list, rm, verify, run, chat, serve, convert, start, tokenize, detok, vocab, selftest. New in 0.11.0: the diffusion decode is 3x faster (llada2.1-mini q8_0, 160 tokens with editing: 33.7s to 11.5s on an RTX 4090) without changing a single generated token: the MoE FFN runs fully on-device (a router kernel writes the assignment table straight to device memory and one batched matvec covers all 256 expert assignments where each (position, expert) pair used to be its own launch), greedy decode reduces argmax + confidence on the GPU so a denoise step downloads 2 floats per position instead of the whole vocab row, q8_0 weights are repacked once at load into a split per-row layout whose 4-byte-aligned quants the matvecs read as ints and float4s (they were load-instruction-bound, not bandwidth-bound; the accumulation order is preserved bit-for-bit), the output projection uses a batch-reuse kernel that reads each weight element once per 32-position tile (7x on the 157k-row vocab matrix, where the weight busts L2 and per-position re-reads are pure DRAM), and a cleanly-converged block skips its freeze re-eval because the last denoise pass already wrote the identical K/V. NURLLAMA_PROF=1 prints a per-phase GPU time breakdown. 0.12.1 — a correctness fix for the --weights path: a GENUINE Hugging Face llama-family checkpoint stores its rotary q/k lanes half-split, but the NORM rope kernel rotates adjacent lanes (llama.cpp permutes at GGUF-conversion time — the safetensors path never did), so every real HF llama/SmolLM/TinyLlama checkpoint ran with misrotated attention and produced fluent-looking garbage; the loader now applies the converter's interleave to q/k (weights and biases, rotary span only) at load, NEOX architectures untouched, and `finetune --merged` writes TRUE HF lane order so its output is a genuine HF checkpoint (merged files written by 0.12.0 carry the pre-permuted order — regenerate them). New in 0.12.0: `nurllama finetune <model.gguf> <data.txt>` — the engine now TRAINS: LoRA adapters (rank/alpha/steps/lr/seq flags) learned on the GPU over the grad autograd tape, the whole transformer forward + cross-entropy expressed as one bit-exactness-disciplined graph replay where frozen base weights cost no gradient memory or backward compute; the corpus round-robins through fixed-size windows with two device uploads per switch; adapters save as safetensors; --merged writes a full merged-model safetensors runnable via --weights, proven by a wiring oracle against the inference engine (top-1 identical, 2e-7 on the top logit) and by the merged model reproducing its training sentence verbatim through greedy decode. Fixed along the way: an HF-name collision in the --weights path (HF llama/qwen2 call the pre-FFN norm post_attention_layernorm; gemma3 uses that name for its EXTRA attention-output norm) silently enabled a spurious per-layer normalization for every real HF llama/qwen2 checkpoint — the mapping is now architecture-aware. 0.11.1: NURLLAMA_PROF also prints a load-phase breakdown ([load] gguf_open/tok/gpu_open/kernels/weights_upload with the upload's copy vs repack split), and model uploads ride gpu 0.9.0's pinned-staged parallel-stripe path (a 17 GB model loads ~0.3s faster; the remaining load cost is the host's own page-cache read bandwidth). New in 0.13.0: the qwen3 architecture — Qwen3 is the llama shape with four differences, every one of which is SILENT when wrong (the model still runs and still emits fluent text): NEOX rotary rather than adjacent-lane NORM, no Q/K/V bias, a head_dim that is STATED rather than n_embd/n_head (Qwen3-0.6B is 16x128 = 2048 against an n_embd of 1024, so the attention projections do not land back on n_embd), and a per-head RMSNorm on Q and K before the rotation. Inference is checked against an independent numpy implementation reading the same GGUF (tests/qwen3_ref.py): identical argmax and top-5, max logit delta 5.1e-5 against a span of 31.6, and a 20-token greedy continuation that is text-identical. Training carries the same shape: the LoRA tape applies the per-head Q/K norm too, `--merged` writes the q_norm/k_norm weights so a merged model is the same model, and the wiring oracle (tape top-1 == inference-engine top-1) closes the chain numpy -> engine -> tape. Also fixed: an unsupported-architecture error printed freed bytes for the architecture name — the message was built after the GGUF it borrows from was closed. Also new in 0.13.0: `finetune --stream`. Eagerly, ft_open lifts every base weight into f64 host tensors and the tape clones each into its own const, so the host peak is two copies of the whole model at the moment of capture — 13.8 GB for Qwen3-0.6B, 2.9x the model's own f64 size, which puts anything past ~1B out of reach of a 31 GB machine. A frozen weight is read exactly once, when the capture uploads it, so with --stream ft_open reads the SHAPES, ft_graph declares each base weight as one of grad 0.9's lazy consts, and ft_stream_upload fills each device buffer straight from the GGUF afterwards, freeing it before the next: the same run in 6.1 GB, with a step-0 loss identical to the last bit on both the f64 and f32 replays. The values go through the same loader the eager path uses, NORM-rope un-permute included, so there is one definition of a base weight rather than two that must agree. New in 0.14.0: finetune --checkpoint FILE [--save-every N] [--resume] — a crash five hours into a run no longer costs the run. Every N steps (default 200) the trainer writes one safetensors file holding the LoRA parameter values, both Adam moment vectors and the step counters — everything the update rule reads — to FILE.tmp, then rename(2)s it over FILE, so a crash mid-write leaves the previous checkpoint intact. --resume loads it and continues the schedule exactly where it stopped (the window round-robin is a function of the step index); with --mixed or --f32 the f32 device storage makes the checkpoint a lossless round-trip, so a killed-and-resumed run replays the uninterrupted one bit for bit — verified by byte-identical adapter files. A final checkpoint is written after the last step, so a crash during the adapter save or the merge resumes straight into them without retraining; a checkpoint whose model shape or rank does not match the run aborts instead of silently training over it, and --resume with no checkpoint file starts fresh, which makes one command line idempotent: run it, and rerun it until the merge lands. New in 0.15.0: finetune --window-stride N — step k trains window (k*N) mod nwin. The sequential default (1) means a run shorter than one epoch reads only the corpus HEAD: a 4000-step seq-56 run over a 7.4M-token corpus sees 3% of it, all from the front. --window-stride 0 picks a golden-ratio stride made coprime with nwin, turning the schedule into a permutation of the windows — any prefix of it samples the whole corpus evenly, a full epoch still visits every window exactly once, and the schedule stays a pure function of the step index, so checkpoint resume replays it bit for bit. The stride is recorded in the checkpoint and a resume under a different one is refused — it would silently change which data the run sees. Verified by tests/finetune_sched_test.nu: permutation property across corpus sizes (the real 132163-window corpus included), stride 1 bit-equal to the legacy order, and a 1%-of-an-epoch prefix reaching both the first and last tenth of the corpus. Also in 0.15.0: finetune --merge-only — skip training, read the adapters file (--out) back and write the merged model (--merged); rank comes from the file's own blk.0.q.lora_a shape and a mismatched file refuses loudly. It merges any shipped adapter file and is the low-memory recovery path after a crash between the adapter save and the merge. The merge path itself lost three memory peaks found the hard way (four OOM kills on a 31 GB box merging a 4B model): the streamed merge now pages each layer's base OUT after its last slot (it paged them in and kept every one — ~850 MB × 36 layers), the merge consumes the 3 GB embedding copy once its bytes are in the writer, and safetensor 0.3.2's writer stores one exact-capacity chunk per tensor and streams them to disk — no concatenation, no doubling, no second copy of 16 GB of tensor bytes. New in 0.16.0: serve --weights FILE — the API twin of run --weights: every model the server opens takes its tensors from the given safetensors file (the GGUF still supplies hyperparameters and tokenizer), so a finetuned/merged checkpoint serves over the ollama-compatible API with one flag. New in 0.17.0: serve [model] — the server takes a default model: the web UI chats with it and an /api request that names no model of its own falls back to it, resolved at startup so a bad name fails immediately instead of as a 500 on the first chat (the bare serve used to host a web UI that answered 500 to every message, since only the start wizard could configure a model). Web-chat failures now carry the actual reason and the UI displays it; a server with no model says so on page load. serve --weights without a model is refused with an explanation (--weights replaces the tensors of a model, it does not select one). run/chat/serve refuse a bare org/repo ref up front with the three working forms instead of downloading a whole safetensors repository and then failing, and a typo'd name lists what a model argument can be instead of surfacing a raw HTTP 401 from the hub."
license = "MIT OR Apache-2.0"

[dependencies]
gguf = { path = "../gguf", version = "^0" }
safetensor = { path = "../safetensor", version = "^0" }
tokenizer = { path = "../tokenizer", version = "^0" }
gpu = { path = "../gpu", version = "^0" }
http = { path = "../http", version = "^0" }
grad = { path = "../grad", version = "^0" }
nn = { path = "../nn", version = "^0" }
gpukit = { path = "../gpukit", version = "^0" }
tensor = { path = "../tensor", version = "^0" }

hub = { path = "../hub", version = "^0" }