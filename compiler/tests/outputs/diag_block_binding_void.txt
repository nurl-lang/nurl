COMPILE FAIL
ERRORS
compiler/tests/diag_block_binding_void.nu:8:5: error: the expression produces no value to bind or assign, but a 'i64' was required. A call returning 'v', an assignment, or a block that is empty or whose last statement yields nothing, all produce no value — bind the result of an expression that produces one.
error: aborting due to 1 previous error
note: each error stopped its declaration — later statements in the same '@'/'%' were not checked, so this count is a lower bound. Fix these, then re-run.
