COMPILE FAIL
ERRORS
compiler/tests/borrow_strict_ret_alias_forward.nu:18: error: 'a' may already be freed here — it was conditionally consumed (one branch only) at line 16; this second free double-frees on that path (strict-borrowck; restructure so exactly one path frees it)
    ( vec_free [i] a )
error: compilation aborted — 1 borrow-checker violation (re-run with --no-borrowck to bypass)
