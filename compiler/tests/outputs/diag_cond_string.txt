COMPILE FAIL
ERRORS
compiler/tests/diag_cond_string.nu:8:9: error: this condition has pointer type 's' — a condition must be 'b', or an integer (tested non-zero); a pointer/string has no truth value in NURL. Test the property you actually mean: emptiness '? != 0 ( nurl_str_len p ) { … }', or null-ness '? != 0 ( # i p ) { … }'.
    ? y { ^ 1 } {}
        ^
error: aborting due to 1 previous error
note: each error stopped its declaration — later statements in the same '@'/'%' were not checked, so this count is a lower bound. Fix these, then re-run.
