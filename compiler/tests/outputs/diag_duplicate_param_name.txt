COMPILE FAIL
ERRORS
compiler/tests/diag_duplicate_param_name.nu:8:13: error: duplicate parameter name 'a' — each parameter of a declaration needs its own name; the body can only reach one of them, and the emitted function would declare the same argument twice. Rename one.
@ f i a i a → i { ^ a }
            ^
error: aborting due to 1 previous error
note: each error stopped its declaration — later statements in the same '@'/'%' were not checked, so this count is a lower bound. Fix these, then re-run.
