COMPILE FAIL
ERRORS
compiler/tests/diag_try_option_in_result_fn.nu:23:5: error: the try operator propagates the failure unchanged, and an option and a result are not the same shape: this '\' received an option ('?T') while the enclosing function returns a result ('!T E'). Convert it explicitly — ( res_ok r ) turns a result into an option by DROPPING the error, ( opt_ok_or o err ) turns an option into a result with an error you choose — then try the converted value.
    ^ @ !i i { T v }
    ^
error: aborting due to 1 previous error
note: each error stopped its declaration — later statements in the same '@'/'%' were not checked, so this count is a lower bound. Fix these, then re-run.
