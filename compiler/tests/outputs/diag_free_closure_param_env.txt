COMPILE FAIL
ERRORS
compiler/tests/diag_free_closure_param_env.nu:7:30: error: this frees the env of closure parameter 'f', which the caller owns and drops itself, so the env would be freed twice - delete this call. A function only borrows the closures it is passed; one that keeps a closure stores a copy (docs/MEMORY.md 7.4).
    ( nurl_free # s # *u f 1 )
                             ^
error: aborting due to 1 previous error
note: each error stopped its declaration — later statements in the same '@'/'%' were not checked, so this count is a lower bound. Fix these, then re-run.
