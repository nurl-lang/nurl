COMPILE FAIL
ERRORS
compiler/tests/borrow_strict_handle_second_name_more.nu:46: error: 'a' may already be freed here — its handle is one of the values a '?' selected between at line 44; this second free double-frees on that path (strict-borrowck; restructure so exactly one path frees it)
    ( vec_free [i] a )
compiler/tests/borrow_strict_handle_second_name_more.nu:54: error: 'a' is consumed here, but its value was stored into an owner at line 52 (an aggregate literal, or a call that keeps it, like vec_push) — that owner drops it now, so this is a second release (a double free). Reading 'a' is fine; to hand a value on AND keep one in the owner, store a copy (string_clone / vec_clone).
    ( vec_free [i] a )
compiler/tests/borrow_strict_handle_second_name_more.nu:62: error: 'a' may already be freed here — a closure that captured it frees it at line 61; this second free double-frees on that path (strict-borrowck; restructure so exactly one path frees it)
    ( vec_free [i] a )
error: compilation aborted — 3 borrow-checker violations (re-run with --no-borrowck to bypass)
