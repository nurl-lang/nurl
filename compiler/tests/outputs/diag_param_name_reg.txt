COMPILE FAIL
ERRORS
compiler/tests/diag_param_name_reg.nu:7:7: error: parameter name 'r0' is reserved: a by-value parameter keeps its name as its LLVM SSA register, and the code generator's temporaries are %r0, %r1, … in that same namespace — an 'r' followed by digits collides with one of them. Rename the parameter (r0 → r_0, or something that says what it holds).
@ f i r0 → i { ^ + r0 1 }
      ^
error: aborting due to 1 previous error
note: each error stopped its declaration — later statements in the same '@'/'%' were not checked, so this count is a lower bound. Fix these, then re-run.
