COMPILE FAIL
ERRORS
compiler/tests/diag_agg_lit_too_many_values.nu:10:24: error: too many values in this aggregate literal: value 3 has no slot — an option is a tag and one payload ('@ ?i { T 7 }'), a result a tag and one payload ('@ !i E { F e }'). If the payload looks right, a prefix operator inside it has one operand too many and the surplus spilled here: count the operands (fixed arity, no closing bracket). The aggregate shape is '{ i1, i64 }'.
    ^ @ ?i { T + n 2 2 }
                       ^
error: aborting due to 1 previous error
note: each error stopped its declaration — later statements in the same '@'/'%' were not checked, so this count is a lower bound. Fix these, then re-run.
