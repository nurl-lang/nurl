COMPILE FAIL
ERRORS
compiler/tests/diag_foreach_not_iterable.nu:11:11: error: '~ e <values> { … }' iterates a slice or a Vec, but this one is of type 'i'. Take a slice of it ('[T ptr len'), build a Vec, or — if a counting loop was meant — write the while form: '~ < i n { … }'.
    ~ e k { = k 1 }
          ^
error: aborting due to 1 previous error
note: each error stopped its declaration — later statements in the same '@'/'%' were not checked, so this count is a lower bound. Fix these, then re-run.
